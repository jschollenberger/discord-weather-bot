discord.py>=2.0
aiohttp>=3.8
tzdata      # Windows only: IANA timezone database for zoneinfo (no-op on Linux/macOS)

# Optional — enables sunrise/sunset in /conditions; the bot runs fine without it
astral
